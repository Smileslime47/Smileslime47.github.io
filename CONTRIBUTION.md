# 贡献指南

感谢你愿意为本项目贡献内容或代码。为了让变更易于审查、回退和维护，请在提交前遵循以下约定。

## 开始开发

项目使用 Node.js 20、Vue 3、TypeScript 和 Less。

```bash
npm ci
npm run dev
```

请从最新的 `main` 分支创建工作分支，并尽量让每次修改只解决一个明确问题。不要在同一提交中混入无关格式调整、构建产物或编辑器配置。

## 添加或修改文章

文章存放在 `src/posts` 目录中，分类由目录结构决定。文章 frontmatter 至少应包含标题和日期，按需添加标签：

```yaml
---
title: 文章标题
date: 2026/09/22
tags:
  - 示例标签
---
```

新增、删除、重命名文章，或修改文章 frontmatter 后，请更新并核对元数据清单：

```bash
npm run generate:post-meta
```

`npm run dev` 和 `npm run build` 会自动执行该步骤。提交前仍应检查 `src/service/posts/meta-manifest.generated.ts`，确保只包含与本次文章变更相关的内容。

文章图片应放在对应分类下的 `image` 目录中，并使用相对路径引用。移动文章时，需要同步移动图片并检查正文中的资源路径。

## 提交前检查

提交前至少运行一次完整构建：

```bash
npm run build
```

同时确认：

- 页面在桌面端和移动端均能正常使用。
- 没有提交 `dist`、编辑器缓存等无关产物。
- 中文内容编码为 UTF-8，且没有引入 BOM 或乱码。
- 工作区中没有被意外纳入的无关文件。

## 约定式提交

提交信息采用约定式提交风格，并使用中文说明：

```text
类型: 中文说明
```

本项目允许以下类型：

| 类型 | 用途 |
| --- | --- |
| `feat` | 新增功能、页面、文章或用户可见能力 |
| `fix` | 修复缺陷、错误行为或内容问题 |
| `perf` | 提升加载、渲染或运行性能 |
| `style` | 调整样式或不影响逻辑的格式 |
| `refactor` | 重构代码且不改变已有行为 |
| `chore` | 构建、依赖、工具链和仓库维护 |

示例：

```text
feat: 添加文章标签筛选
fix: 修复归档页日期排序
perf: 减少文章列表首屏请求
style: 优化移动端文章间距
refactor: 提取文章日期解析逻辑
chore: 更新元数据生成脚本
```

提交说明应简洁、明确，并描述本次提交带来的结果。一个提交只承担一个主要目的；如果存在不兼容变更，请在提交正文中使用 `BREAKING CHANGE:` 说明影响和迁移方式。

## 提交合并请求

合并请求应说明变更目的、主要实现和验证方式。涉及界面调整时请附上截图，涉及行为变化时请补充复现步骤或测试结果。提交前请再次确认变更范围清晰，且没有覆盖其他贡献者尚未提交的工作。
